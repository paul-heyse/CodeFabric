id: provider-observation-boundary-only
snapshots:
  ? |
    fn bypass(writer: &mut Writer, request: Request) {
        writer.replace_owner_rows(request).unwrap();
    }
  : labels:
    - source: writer.replace_owner_rows(request).unwrap()
      style: primary
      start: 55
      end: 98
  ? |
    use crate::fabric::{OverlayMutation, WorkspaceFabric};
  : labels:
    - source: use crate::fabric::{OverlayMutation, WorkspaceFabric};
      style: primary
      start: 0
      end: 54

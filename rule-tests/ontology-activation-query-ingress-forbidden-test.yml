id: ontology-activation-query-ingress-forbidden
valid:
  - |
      fn query_ontology_version() -> String {
          current_ontology_version()
      }
invalid:
  - |
      fn activate_ontology(candidate_id: String) {
          activate(candidate_id);
      }

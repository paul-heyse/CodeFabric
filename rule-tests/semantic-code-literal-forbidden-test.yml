id: semantic-code-literal-forbidden
valid:
  - |
      fn operand(code: i32) {
          lit(ScalarValue::Int32(Some(code)));
      }
invalid:
  - |
      fn operand() {
          lit(ScalarValue::Int32(Some(120)));
      }

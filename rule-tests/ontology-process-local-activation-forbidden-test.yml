id: ontology-process-local-activation-forbidden
valid:
  - |
      struct DurableOntologyDecisionStore {
          connection: Connection,
      }
invalid:
  - |
      struct OntologyActivationState {
          active_candidate: Option<String>,
      }
  - |
      async fn activate_stage2b(candidate: Candidate) {
          ACTIVE.store(candidate);
      }

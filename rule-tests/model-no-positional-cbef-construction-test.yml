id: model-no-positional-cbef-construction
valid:
  - |
      fn identity(fields: EntityFields) {
          let record = entity(fields).unwrap();
      }
invalid:
  - |
      fn identity(fields: Vec<CbefField>) {
          let record = CbefRecord { domain: IdentityDomain::Entity, fields };
      }

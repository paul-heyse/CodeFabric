//! Intentionally invalid graph: the governance test requires cargo-deny to reject it.

// The manifest graph is the fixture. This target exists only so Cargo can resolve it;
// governance never compiles or executes the deliberately duplicated dependencies.

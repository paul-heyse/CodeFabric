artifact_id: codefabric.faults.fault-point-registry
artifact_kind: registry
version: "1.0"
compatible_suite_major: 1
status: released
canonical_digest: "b3:0ea80137983fbad1c76693054a472a5a832e12c66cac7e5f744a854b859fa579"
digest_projection: yaml-ac-g-53-v1
generator_revision: codefabric-contracts/1.0
records:
  - code: SOURCE_CAPTURE_AFTER_FIRST_READ
    owner: fact-generation
    allowed_actions: [mutate_fixture, block_on_barrier]
    production_exposable: false
    expected_invariants: [no-false-stable-image, bounded-retry]
    scenarios: [source-capture-concurrent-mutation]
  - code: SOURCE_BLOB_BEFORE_RENAME
    owner: lifecycle
    allowed_actions: [return_error, terminate_process]
    production_exposable: false
    expected_invariants: [no-partial-blob, content-addressed-name]
    scenarios: [source-blob-atomic-publication]
  - code: SOURCE_LEASE_AFTER_ORPHAN
    owner: lifecycle
    allowed_actions: [return_error, block_on_barrier]
    production_exposable: false
    expected_invariants: [orphan-grace-preserved, live-holder-preserved]
    scenarios: [source-lease-restart-recovery]
  - code: SOURCE_GC_BEFORE_DELETE
    owner: lifecycle
    allowed_actions: [return_error, block_on_barrier]
    production_exposable: false
    expected_invariants: [live-holder-preserved, idempotent-garbage-collection]
    scenarios: [source-blob-concurrent-release-gc]
  - code: OVERLAY_REBASE_AFTER_FLUSH_CAPTURE
    owner: data-fabric
    allowed_actions: [return_error, terminate_process]
    production_exposable: false
    expected_invariants: [captured-overlay-immutable, durable-pointer-unchanged]
    scenarios: [overlay-three-snapshot-rebase]
  - code: OVERLAY_REBASE_AFTER_PUBLICATION_CAS
    owner: data-fabric
    allowed_actions: [return_error, terminate_process]
    production_exposable: false
    expected_invariants: [durable-pointer-recoverable, old-snapshot-remains-leasable]
    scenarios: [overlay-three-snapshot-rebase]
  - code: OVERLAY_REBASE_BEFORE_SNAPSHOT_ACTIVATION
    owner: data-fabric
    allowed_actions: [return_error, terminate_process]
    production_exposable: false
    expected_invariants: [effective-content-validated, active-snapshot-unchanged]
    scenarios: [overlay-three-snapshot-rebase]
  - code: ONTOLOGY_BEFORE_CANDIDATE_VALIDATION
    owner: data-fabric
    allowed_actions: [return_error, terminate_process]
    production_exposable: false
    expected_invariants: [candidate-not-accepted, active-pointer-unchanged]
    scenarios: [ontology-stage2b-atomic-activation]
  - code: ONTOLOGY_AFTER_CANDIDATE_VALIDATION
    owner: data-fabric
    allowed_actions: [return_error, terminate_process]
    production_exposable: false
    expected_invariants: [validated-candidate-not-active, active-pointer-unchanged]
    scenarios: [ontology-stage2b-atomic-activation]
  - code: ONTOLOGY_BEFORE_ACCEPTANCE_RECORD
    owner: data-fabric
    allowed_actions: [return_error, terminate_process]
    production_exposable: false
    expected_invariants: [owner-acceptance-absent, active-pointer-unchanged]
    scenarios: [ontology-stage2b-atomic-activation]
  - code: ONTOLOGY_AFTER_ACCEPTANCE_RECORD
    owner: data-fabric
    allowed_actions: [return_error, terminate_process]
    production_exposable: false
    expected_invariants: [acceptance-and-pointer-atomic, active-pointer-unchanged]
    scenarios: [ontology-stage2b-atomic-activation]
  - code: ONTOLOGY_BEFORE_POINTER_ADVANCE
    owner: data-fabric
    allowed_actions: [return_error, terminate_process]
    production_exposable: false
    expected_invariants: [accepted-candidate-not-partially-active, active-pointer-unchanged]
    scenarios: [ontology-stage2b-atomic-activation]
  - code: ONTOLOGY_AFTER_POINTER_ADVANCE
    owner: data-fabric
    allowed_actions: [return_error, terminate_process]
    production_exposable: false
    expected_invariants: [pointer-transaction-rollback, active-leases-preserved]
    scenarios: [ontology-stage2b-atomic-activation]
  - code: ONTOLOGY_ACTIVATION_BEFORE_COMMIT
    owner: data-fabric
    allowed_actions: [return_error, terminate_process]
    production_exposable: false
    expected_invariants: [acceptance-and-pointer-atomic, active-pointer-unchanged, request-retry-safe]
    scenarios: [ontology-activation-state-transaction]
  - code: ONTOLOGY_ACTIVATION_AFTER_COMMIT_RESPONSE_LOST
    owner: data-fabric
    allowed_actions: [return_error, terminate_process]
    production_exposable: false
    expected_invariants: [committed-pointer-recoverable, request-idempotent, no-double-activation]
    scenarios: [ontology-activation-restart-recovery]
  - code: ONTOLOGY_ACTIVATION_CAS_CONFLICT
    owner: data-fabric
    allowed_actions: [return_error, block_on_barrier]
    production_exposable: false
    expected_invariants: [single-pointer-winner, losing-candidate-remains-proved, active-pointer-monotonic]
    scenarios: [ontology-activation-concurrency]
  - code: MUTATION_AFTER_DELETE_COMMIT
    owner: data-fabric
    allowed_actions: [return_error, terminate_process]
    production_exposable: false
    expected_invariants: [owner-replacement-recoverable, no-mixed-owner-generation]
    scenarios: [owner-replacement-recovery]
  - code: MUTATION_BEFORE_APPEND_COMMIT
    owner: data-fabric
    allowed_actions: [return_error, terminate_process]
    production_exposable: false
    expected_invariants: [owner-replacement-recoverable, delete-commit-observable]
    scenarios: [owner-replacement-recovery]
  - code: PUBLICATION_AFTER_STAGING
    owner: data-fabric
    allowed_actions: [return_error, terminate_process]
    production_exposable: false
    expected_invariants: [staging-is-not-current, active-pointer-unchanged]
    scenarios: [publication-restart-recovery]
  - code: PUBLICATION_AFTER_OWNER_WRITES
    owner: data-fabric
    allowed_actions: [return_error, terminate_process]
    production_exposable: false
    expected_invariants: [owner-writes-not-current, active-pointer-unchanged]
    scenarios: [publication-restart-recovery]
  - code: PUBLICATION_AFTER_MANIFEST_WRITE
    owner: data-fabric
    allowed_actions: [return_error, terminate_process]
    production_exposable: false
    expected_invariants: [manifest-without-pointer-invisible, active-pointer-unchanged]
    scenarios: [publication-restart-recovery]
  - code: PUBLICATION_BEFORE_POINTER_COMMIT
    owner: data-fabric
    allowed_actions: [return_error, terminate_process]
    production_exposable: false
    expected_invariants: [candidate-validated-before-activation, active-pointer-unchanged]
    scenarios: [publication-restart-recovery]
  - code: PUBLICATION_AFTER_POINTER_COMMIT
    owner: data-fabric
    allowed_actions: [return_error, terminate_process]
    production_exposable: false
    expected_invariants: [committed-pointer-recoverable, publication-idempotent]
    scenarios: [publication-restart-recovery]
  - code: QUERY_RESULT_INSERTION
    owner: data-fabric
    allowed_actions: [return_error]
    production_exposable: false
    expected_invariants: [terminal-fallback-preserves-evidence, no-terminal-before-journal]
    scenarios: [query-terminal-artifact-closure]
  - code: QUERY_PRIMARY_PAYLOAD_PERSISTENCE
    owner: data-fabric
    allowed_actions: [return_error, terminate_process]
    production_exposable: false
    expected_invariants: [bounded-fallback-envelope, no-empty-failure-artifact]
    scenarios: [query-terminal-artifact-closure, query-terminal-restart-recovery]
  - code: QUERY_TERMINAL_JOURNAL_COMMIT
    owner: data-fabric
    allowed_actions: [return_error, terminate_process]
    production_exposable: false
    expected_invariants: [no-governed-terminal-claim, conflicting-terminal-fails-closed]
    scenarios: [query-terminal-artifact-closure, query-terminal-restart-recovery]
  - code: PROVIDER_ADMISSION
    owner: fact-generation
    allowed_actions: [return_error, block_on_barrier]
    production_exposable: false
    expected_invariants: [accepted-handle-before-capacity-wait, rejected-run-never-launches]
    scenarios: [semantic-provider-admission]
  - code: PROVIDER_CHILD_LAUNCH
    owner: fact-generation
    allowed_actions: [return_error, terminate_process]
    production_exposable: false
    expected_invariants: [untrusted-never-launches-unconfined, launch-failure-terminal]
    scenarios: [semantic-provider-launch]
  - code: PROVIDER_HANDSHAKE
    owner: fact-generation
    allowed_actions: [return_error, terminate_process]
    production_exposable: false
    expected_invariants: [identity-mismatch-fails-closed, no-output-before-handshake]
    scenarios: [semantic-provider-handshake]
  - code: PROVIDER_STAGE_CREATION
    owner: lifecycle
    allowed_actions: [return_error, terminate_process]
    production_exposable: false
    expected_invariants: [no-live-workspace-link, output-root-separate]
    scenarios: [semantic-provider-stage]
  - code: PROVIDER_CHUNK_WRITE
    owner: fact-generation
    allowed_actions: [return_error, terminate_process]
    production_exposable: false
    expected_invariants: [partial-chunk-invisible, credit-window-bounded]
    scenarios: [semantic-provider-chunk]
  - code: PROVIDER_CHUNK_ACCEPT
    owner: fact-generation
    allowed_actions: [return_error, block_on_barrier]
    production_exposable: false
    expected_invariants: [accepted-chunk-digest-verified, credit-return-after-acceptance]
    scenarios: [semantic-provider-chunk]
  - code: PROVIDER_CHUNK_REJECT
    owner: fact-generation
    allowed_actions: [return_error, block_on_barrier]
    production_exposable: false
    expected_invariants: [rejected-chunk-not-staged, rejection-terminal-evidence]
    scenarios: [semantic-provider-chunk]
  - code: PROVIDER_TERMINAL_VERIFY
    owner: fact-generation
    allowed_actions: [return_error, terminate_process]
    production_exposable: false
    expected_invariants: [no-publication-before-terminal, output-fingerprint-verified]
    scenarios: [semantic-provider-terminal]
  - code: PROVIDER_CANCELLATION
    owner: lifecycle
    allowed_actions: [return_error, block_on_barrier]
    production_exposable: false
    expected_invariants: [cancellation-bounded, post-cancel-output-discarded]
    scenarios: [semantic-provider-cancellation]
  - code: PROVIDER_KILL
    owner: lifecycle
    allowed_actions: [return_error, terminate_process]
    production_exposable: false
    expected_invariants: [process-group-terminated, terminal-journal-preserved]
    scenarios: [semantic-provider-cancellation]
  - code: PROVIDER_CLEANUP
    owner: lifecycle
    allowed_actions: [return_error, block_on_barrier]
    production_exposable: false
    expected_invariants: [immutable-input-retained-by-lease, writable-stage-reclaimable]
    scenarios: [semantic-provider-cleanup]
  - code: PROVIDER_JOURNAL_TRANSITION
    owner: lifecycle
    allowed_actions: [return_error, terminate_process]
    production_exposable: false
    expected_invariants: [failed-transition-does-not-advance-state, terminal-evidence-not-silently-lost]
    scenarios: [semantic-provider-journal]
owner_acceptance:
  approver: lifecycle-owner
  accepted_at: "2026-08-21"
  construction_rule: Append-only deterministic source, provider, mutation, publication, overlay, ontology activation, and query-terminal durability seams
  source_digest: "b3:2e17092b92aa3d34e76229820af9482e66c10163c59aee5a667164e493f3e951"
